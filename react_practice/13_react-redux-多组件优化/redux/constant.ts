/**
 * 该模块用于定义count_action对象中type的常量类型
 */
const INCREMENT = 'increment' // 加
const DECREMENT = 'decrement' // 减
const INCREMENTIFODD = 'incrementIfOdd' // 奇数加
const INCREMENTIFASYNC = 'incrementIfAsync' // 异步加
/**
 * 该模块用于定义person_action对象中type的常量类型
 */
const ADDPERSON = 'addPerson'

const ADDMIX = 'addMix'


export {
  INCREMENT,
  DECREMENT,
  INCREMENTIFODD,
  INCREMENTIFASYNC,
  ADDPERSON,
  ADDMIX
}