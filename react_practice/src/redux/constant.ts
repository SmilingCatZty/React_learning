/**
 * 该模块用于定义action对象中type的常量类型
 */
const INCREMENT = 'increment' // 加
const DECREMENT = 'decrement' // 减
const INCREMENTIFODD = 'incrementIfOdd' // 奇数加
const INCREMENTIFASYNC  = 'incrementIfAsync' // 异步加

export {
  INCREMENT,
  DECREMENT,
  INCREMENTIFODD,
  INCREMENTIFASYNC
}